const React = require('react'),
      Register = require('../../shared/register.jsx'),
      mountNode = document.getElementById('app');

// React.render(new Register({}), mountNode);

// React.render(<ReactApp />, mountNode);
